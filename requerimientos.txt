# Requisitos para el proyecto ETL - Airbnb México
# Instalar con: pip install -r requirements.txt
# Tener una base de datos en MongoDB con los csv de Airbnb Mexico cargados. (Esta es la base de datos donde se extraen los datos iniciales)
# Tener una base de datos en SQL Server con el nombre de AirbnbMexico o el nombre que mas se le adecude. (Las tablas las crear el mismo proceso de ETL)

# Manipulación de datos
pandas>=2.0.0
numpy>=1.24.0

# Conexión a MongoDB
pymongo>=4.6.0

# Conexión a SQL Server
pyodbc>=5.0.0

# Opcional: Para mejor visualización en consola
colorama>=0.4.6